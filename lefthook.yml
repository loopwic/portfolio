pre-commit:
  parallel: true
  commands:
    lint_staged:
      run: bunx lint-staged

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit "$1"

pre-push:
  commands:
    lint:
      run: bun run lint
