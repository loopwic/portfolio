pre-commit:
  parallel: true
  commands:
    lint_staged:
      run: pnpm lint-staged

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit "$1"

pre-push:
  commands:
    lint:
      run: pnpm lint
